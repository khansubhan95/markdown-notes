const initialState = {
  notes: [],
  selectedNoteId: null,
  selectedNoteTitle: "",
  selectedNoteContent: "",
  formError: false
};

const reducer = (state = initialState, action) => {
  return state
}

export default reducer
